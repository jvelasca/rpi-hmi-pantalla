# Display app dependencies (Pygame DRM + evdev touch)
# Instalar en el venv de la Pi:
#   source /home/pi/rpi_hmi/venv/bin/activate
#   pip install -r display/requirements.txt

pygame>=2.6,<3.0
evdev>=1.7,<2.0
requests>=2.31,<3.0
websocket-client>=1.8,<2.0
