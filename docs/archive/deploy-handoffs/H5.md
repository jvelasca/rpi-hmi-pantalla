# Handoff H5 — Documentación y consistencia de versión

## Resultado
Completado (no parcial). Eliminadas las IPs hardcodeadas de los archivos bajo
mi titularidad y unificado el conteo de tests al número real (**287** pytest
backend+display, **16** frontend Vitest). Añadida `SECURITY_MODE` a
`.env.example`. La referencia de versión `v2.0` de `docs/ARCHITECTURE.md` quedó
alineada con la versión canónica **0.3.0**.

## Ocurrencias encontradas y reemplazos hechos

| Archivo | Ocurrencia | Reemplazo |
|---|---|---|
| `README.md` | `http://192.168.88.211:8000` (1) | `http://<IP_DE_LA_PI>:8000` |
| `README.md` | `278 tests (4 skipped)` (3: badge, tabla, línea CI) | `287 tests` + `16 frontend` (Vitest) |
| `QUICKSTART.md` | `192.168.1.X` (3: ssh, curl, comentario) | `<IP_DE_LA_PI>` |
| `docs/CONTEXT.md` | `192.168.88.211` (7: tabla estado, docs, 3× ssh, api-url, `RPI_HOST`) | `<IP_DE_LA_PI>` |
| `docs/ARCHITECTURE.md` | `v2.0` (1: título) | `(v0.3.0)` |
| `.env.example` | `RPI_HOST=192.168.88.211` (1) | `RPI_HOST=192.168.1.100` + comentario |
| `.env.example` | `SECURITY_MODE` ausente | Añadida `SECURITY_MODE=local` con comentario (`local` / `protected`) |
| `frontend/.env.example` | — (sin cambios) | Ya documenta `VITE_API_URL` correctamente |

## Archivos modificados
Todos [editado], salvo indicación.

- `README.md` — IP hardcodeada → marcador; test count `278` → `287 + 16`.
- `QUICKSTART.md` — `192.168.1.X` → `<IP_DE_LA_PI>`.
- `docs/CONTEXT.md` — 7× `192.168.88.211` → `<IP_DE_LA_PI>`.
- `docs/ARCHITECTURE.md` — título `v2.0` → `(v0.3.0)`.
- `.env.example` — añadida `SECURITY_MODE`; `RPI_HOST` con IP de ejemplo neutra.
- `frontend/.env.example` — sin cambios (ya correcto).
- `docs/deploy/handoffs/H5.md` — [nuevo] este handoff.

## Verificación ejecutada
Ejecutado desde la raíz del repo:

- `grep -rn "192.168.88.211" README.md QUICKSTART.md docs/CONTEXT.md docs/ARCHITECTURE.md` → **sin resultados**.
- `grep -rnE "180|222|277" README.md` → **sin resultados** (solo queda `287`).
- `grep -n "192.168.88.211"` global (`.md`) → solo queda en archivos **fuera de mi titularidad**:
  `docs/deploy/INICIO.md`, `docs/audits/refactor/handoffs/D.md`,
  `docs/audits/refactor/PROMPTS_SUBAGENTES.md`, `PROMPT_CONTINUACION.md`,
  `docs/handoffs/CHAT_4_D_SEGURIDAD.md`, `docs/audits/fase6-persistencia.md`,
  `docs/audits/fase0-seguridad.md`.

No se ejecutó `npm run build` (sanity opcional, no obligatorio; cambio solo de docs/config).

## Decisiones tomadas
- Test count unificado a **287** (pytest backend+display) y **16** (Vitest frontend),
  eliminando el antiguo "278 tests (4 skipped)" y el "4 skipped" (no inventar).
- En `QUICKSTART.md` sustituí `192.168.1.X` por `<IP_DE_LA_PI>` por coherencia con
  el marcador del resto del repo (era un placeholder, no la IP `192.168.88.211`).
- En `.env.example` cambié `RPI_HOST` a `192.168.1.100` (ejemplo neutro válido) en
  lugar de un placeholder literal, para que el `.env` copiado siga siendo parseable.
- `SECURITY_MODE` documentado con sus dos valores reales (`local`/`protected`) tomados
  de `backend/app/config.py`, y el resumen de que en `protected` los mutadores HMI
  (REST + WebSocket) exigen `X-API-Key` (detalle en `docs/SECURITY.md`).
- `frontend/.env.example` no se tocó: ya documenta `VITE_API_URL` y su rol solo-dev
  (en producción el frontend usa `location.host`, mismo origen).

## Riesgos / pendientes
- `docs/CONTEXT.md` conserva su tabla "Estado actual" con el conteo **103/103** y la
  fecha de sesión 2026-08-11 (histórico de checkpoint). El conteo de tests solo era
  requisito DONE para README/QUICKSTART; queda fuera de H5 por si el coordinador
  quiere refrescar el checkpoint completo.
- Los 7 archivos fuera de mi titularidad que aún contienen `192.168.88.211` (listados
  arriba) no se tocaron; corresponden a otros workstreams/históricos.
- `docs/ARCHITECTURE.md` sigue titulándose documento "v0.3.0"; si la intención era
  eliminar toda versión del título (no alinearla), revísese.
- No se validó render del badge de tests (shield.io URL-encoded); el texto visible es
  `tests-287 pytest + 16 vitest`.

## Texto de paso al siguiente agente
Workstream H5 completo y verificado: sin `192.168.88.211` en README/QUICKSTART/CONTEXT/
ARCHITECTURE y sin `180|222|277` en README. Nada pendiente en mi ámbito. Si se retoma,
considerar refrescar la tabla "Estado actual" de `docs/CONTEXT.md` (conteo 103→287 y
fecha) como tarea separada, y evaluar los 7 archivos históricos que aún referencian la
IP `192.168.88.211`.
