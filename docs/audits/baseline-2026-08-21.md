# Baseline verificable — Fase 0 (2026-08-21)

> Punto de partida medido antes de cualquier cambio de refactorización. Se usa
> para detectar regresiones de forma inequívoca en cada fase posterior.

## Estado del repositorio

- **Rama:** `main`.
- **Versión en `VERSION`:** `0.3.4` (sin commitear; la auditoría externa revisó
  `main` en `0.3.3`, commit `17a2967`).
- **Estado git:** gran parte del código de FASE 7 sin trackear
  (`backend/app/*`, `frontend/src/*`, `display/*`, `docs/*`, `tests/*`).
- **Objetivo de versión:** `0.4.0`.

## Entorno local

| Herramienta | Versión |
|---|---|
| Python | 3.13.7 |
| pytest | 8.4.2 |
| ruff | 0.16.3 (`python -m ruff`) |
| mypy | 1.19.1 (`python -m mypy`) |
| Node | 22.14.0 |
| npm | 10.9.2 |

## Resultados de los gates (alcance real del CI)

| Gate | Comando | Resultado |
|---|---|---|
| Backend + display tests | `python -m pytest backend/tests display/tests -q` | **391 passed, 9 skipped** (5 warnings) |
| Frontend tests | `npm test` | **27 passed** (3 archivos) |
| Frontend build | `npm run build` | **OK** (35.96 kB gzip) |
| ruff (CI) | `python -m ruff check backend display scripts --config backend/pyproject.toml` | **All checks passed** |
| mypy (CI) | `python -m mypy app --config-file pyproject.toml` (desde `backend/`) | **Success: no issues in 31 files** |

Notas:

- Los 5 warnings de pytest son `RuntimeWarning` pre-existentes en
  `test_state_manager.py` (coroutine de `AsyncMock` nunca awaited).
- `bandit` y `pip-audit` no se ejecutaron localmente (se instalan en CI); quedan
  pendientes de verificación en la Fase 6.

## Fuera del alcance del CI (detectado, pre-existente)

- `ruff check .` (repo completo): **14 errores**, todos en `diagnostics/`
  (`blink_test.py`, `run_diagnostics.py`), carpetas legacy fuera del alcance del
  CI. Se limpiarán/archivarán en Fase 5.
- `python -m mypy backend display` (alcance ampliado): **526 errores** en
  `backend/tests/` y `display/` (anotaciones ausentes). El CI solo tipa
  `backend/app/`, que está limpio. Decisión a evaluar en Fase 6: ampliar o no el
  alcance de mypy.

## Cómo reproducir

```bash
# Backend + display
python -m pytest backend/tests display/tests -q

# Lint (CI)
python -m ruff check backend display scripts --config backend/pyproject.toml

# Tipado (CI)
cd backend && python -m mypy app --config-file pyproject.toml

# Frontend
cd frontend && npm test && npm run build
```
