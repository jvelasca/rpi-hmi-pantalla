# RPi HMI Backend — Dependencies
# Espejo fiel de backend/pyproject.toml.
# Generado automáticamente — no editar manualmente.
#
# Instalación en Raspberry Pi:
#   pip install -r requirements.txt
#
# Instalación en PC (desarrollo):
#   pip install -r requirements.txt
#   (las dependencias comentadas son opcionales)

# === Runtime (siempre necesarias) ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0
gpiozero>=2.0
pyyaml>=6.0
python-dotenv>=1.0
aiosqlite>=0.20.0
paramiko>=3.4

# === Deploy (solo PC de desarrollo) ===
# paramiko ya incluido en runtime; este grupo es declarativo
#   (pyproject.toml [project.optional-dependencies].deploy)

# === Display (solo Raspberry Pi con TFT físico) ===
# pygame>=2.6
# evdev>=1.7

# === Dev (solo desarrollo) ===
# pytest>=8.0
# pytest-asyncio>=0.24
# pytest-cov>=4.1
# httpx>=0.27
# mypy>=1.14
# ruff>=0.8
